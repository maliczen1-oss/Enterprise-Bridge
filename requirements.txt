# WealthBuilder Bridge — Phase 2.1 dependencies
# Pinned to minor versions for reproducible deployments.
# Upgrade with care: validate the full test suite after any change.

# Web framework
fastapi>=0.115.0,<0.116.0

# ASGI server
uvicorn[standard]>=0.32.0,<0.33.0

# Data validation and serialisation
pydantic>=2.9.0,<3.0.0

# Environment variable loading
python-dotenv>=1.0.0,<2.0.0

# Async HTTP client (used by Phase 2.2+ services)
httpx>=0.27.0,<0.28.0

# Testing
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<0.25.0

# MetaTrader5 terminal Python bindings
MetaTrader5>=5.0.37 ; sys_platform == 'win32'
